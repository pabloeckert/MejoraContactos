# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# Supabase local state
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
test-results/
playwright-report/
coverage/

# Data/ (contactos personales reales de terceros — Pablo y Sindy) NUNCA a
# este repo, público en GitHub. Tiene su PROPIO repo git local (sin
# remoto) para versionado/backup independiente — ver
# motor-contactos/scripts/setup_project.ps1.
Data/
__pycache__/
*.pyc
.pytest_cache/

# motor-contactos/ (2026-08-15): se fusionó el CÓDIGO al repo raíz
# (verificado sin secretos ni datos reales en su historial) — sigue
# teniendo, ADEMÁS, su propio repo git local independiente adentro
# (motor-contactos/.git) para el flujo de trabajo de scripts/handoff.ps1
# entre sesiones; no se toca ni se borra.

# Transcripción de sesión (puede contener datos personales reales) — nunca a git
MejoraContactos.md
