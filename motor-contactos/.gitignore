.venv/
__pycache__/
*.pyc
.pytest_cache/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
.env

# UI nueva (ui/) -- no versionar dependencias ni build output
node_modules/
dist/

# App de escritorio empaquetada (PyInstaller) -- artefacto generado,
# reconstruir con scripts/build_exe.ps1, no versionar el binario.
App/
build/
*.spec

# Documento de auditoría generado (scripts/generar_auditoria.py) --
# artefacto regenerable, no versionar el binario.
motor-contactos-auditoria.docx

# OAuth de Google Contacts -- secretos, nunca a git (ni siquiera al repo local)
credentials.json
token_*.json
